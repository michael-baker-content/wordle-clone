// Bump only for an intentional fresh start for every player.
export const PROGRESS_VERSION = "jacklet-v3";
export const STORAGE_KEY = `${PROGRESS_VERSION}:history`;
export const HELP_KEY = `${PROGRESS_VERSION}:help`;
export const SOUND_KEY = `${PROGRESS_VERSION}:sound`;
